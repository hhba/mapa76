<ol>
  <%@nombres_propios.keys.sort.each{|nombre|%>
    <li><%=link_to(nombre,url_for(:nombre,:name => nombre, :doc_id => params[:doc_id]))%> (<%=@nombres_propios[nombre].length%> menciones)
  <%}%>
</ol>
