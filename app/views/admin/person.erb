<h1><%=@name%></h1>
<ul>
    <%@persons.each{|person|%>
        <li><%=person.name%>
        <ol>
            <%person.documents.each{|doc|%>
                <li><%= link_to doc.title, url_for(:doc_admin_person,:doc_id => doc.id, :id => person.id)%>
            <%}%>
        </ol>
    <%}%>
</ul>
